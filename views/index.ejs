<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Url shortner</title>
</head>
<body>
    <header>
        <div class="header">
            <div class="left-content">
                <h1 class="title">Acorta tus enlaces de forma sencilla.</h1>
                <form action="/" method="post">
                    <p>Pega la URL aquí</p>
                    <input class="url" type="url" name="url" placeholder=""/>
                    <input class= "submit" type="submit" value="Create url"/>
                </form>
            </div>
            <img class="image" src="./images/link.jpg">
        </div>
    </header>

    <% if (locals.error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <div class="short-url">
        <% if (locals.short_url) { %>
            <a href="<%= short_url %>"> <%= short_url %> </a>
        <% } %>
    </div>
</body>
</html>